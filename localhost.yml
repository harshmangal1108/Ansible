---
 - hosts: localhost
   tasks:
    - name: runing date command
      command: date ##it will run date command  ##command is module to run commands
      name: mkdir
      command: mkdir /etc/ansible/tryl
    - name: print message
      debug: msg="Hello World this is Ansible" ##it is debug module
